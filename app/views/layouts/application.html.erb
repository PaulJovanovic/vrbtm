<!DOCTYPE html>
<html>
<head>
  <title>Vrbtm</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:300,400,700' rel='stylesheet' type='text/css'/>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= link_to "#" do %>
    <div class="pvxxxs c-16 bg-teal text-center">
      This website is currently in beta.  We encourage all to try out vrbtm and provide feedback.
    </div>
  <% end %>
  <% if signed_in? %>
    <div class="bg-2">
      <div class="container phxs">
        <div class="dvm lhm">
          <div class="pull-right">
            <%= link_to user_path(current_user), class: "c-16" do %>
              <%= image_tag avatar_image(current_user, :small), class: "daxs" %>
              <%= current_user.name %>
            <% end %>
            &nbsp;
            <%= link_to "logout", destroy_user_session_path, method: :delete, class: "c-16" %>
          </div>
          <div>
            <div class="pull-left">
              <%= link_to root_path do %>
                <i class="icon-vrbtm fsl c-16" style="vertical-align: middle"></i>
              <% end %>
            </div>
            <div class="relative mlxl c-10 fsxxxs pull-left hidden-xs">
              <%= text_field_tag 'user_search', nil, placeholder: "Search people", class: "phxs mtxs bg-6 dvxs lhxs bras js-people-search" %>
              <div class="absolute right top mrxxxs">
                <i class="fa fa-search"></i>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
  <%= yield %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
</body>
</html>
